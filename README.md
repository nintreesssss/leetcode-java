# LeetCode Java算法练习

这个仓库会包含每日LeetCode练习，旨在提升算法思维.

**语言**：Java

## 文件夹结构

- 解题文件以 LeetCode 题目编号命名（例如 TwoSum.java）.

- `src`: 这个文件夹存放源代码.
- `lib`: 这个文件夹维护依赖性.

已编译的输出文件会在 `bin` 文件夹里.

> `.vscode/settings.json`用来自定义文件夹结构.
